import React from 'react';

const Axios =() => {
    return (
        <div className="Wrapper">
            <input type="text" name="name" placeholder="Enter your name" className="TodoInput"/>
            <button className="addBtn">Add</button>
        </div>
    );
};

export default Axios;